stages:
  - test

.deploy-trigger: &deploy-trigger
  trigger:
    project: sbidoul/sandbox
    branch: deploy

test:
  <<: *deploy-trigger
  stage: test
  variables:
    DEPLOY_ENV: theenv
    DEPLOY_COMMIT_REF_NAME: $CI_COMMIT_REF_NAME
  when:
    manual

review:
  <<: *deploy-trigger
  stage: test
  variables:
    DEPLOY_ENV: review
    DEPLOY_COMMIT_REF_NAME: $CI_COMMIT_REF_NAME
  when:
    manual
