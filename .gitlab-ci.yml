stages:
  - deploy

test:
  stage: test
  tags:
    - sbi
  script:
    - ls -al $PIP_CACHE_DIR
    - ls -al /home/gitlab-runner/.cache/git-autoshare
    - ls -al /tmp/gitlab-runner-cache
    - df -hT
    - psql -l 
